{% macro table(rows, cols) %}
  <table>
    {% for r in rows %}
      <tr>
      {% for c in cols %}
        <td>{{ caller(r, c) }}</td>
      {% endfor %}
      </tr>
    {% endfor %}
  </table>
{% endmacro %}

{% call(x, y) table(['a', 'b', 'c'], ['1', '2']) %}
  {{ x }} - {{ y }}
{% endcall %}
